# parent

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build parent` to build the library.

## Running unit tests

Run `nx test parent` to execute the unit tests via [Jest](https://jestjs.io).
